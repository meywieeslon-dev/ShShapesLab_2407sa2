<Window x:Class="ShapesLab_Final.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Factory Method - Shapes Lab" Height="450" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

       
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Выберите цвет фигур:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <ComboBox x:Name="ColorComboBox" Width="200" HorizontalAlignment="Left"
                      SelectionChanged="ColorComboBox_SelectionChanged">
                <ComboBoxItem Content="Красный"/>
                <ComboBoxItem Content="Синий"/>
                <ComboBoxItem Content="Зелёный"/>
            </ComboBox>
        </StackPanel>

        
        <Button Grid.Row="1" Content="Создать фигуры" Width="150" Height="30" 
                Margin="10" Click="CreateButton_Click"/>

       
        <StackPanel x:Name="ShapesPanel" Grid.Row="2" Margin="10" 
                    Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>